{
  "deployment": {
    "build": "node build.js",
    "start": "node server.js",
    "port": 5000,
    "healthCheck": "/health",
    "description": "TypeScript WebRTC SDK with LiveKit integration"
  },
  "scripts": {
    "start": "node server.js",
    "build": "node build.js",
    "deploy": "node build.js && node server.js",
    "health": "curl -f http://localhost:5000/health || exit 1"
  }
}